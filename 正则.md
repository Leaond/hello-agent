# Python re 正则完整极简梳理
## 一、基础用法总入口
```python
import re
# 常用三个核心函数
re.search()   # 全文查找，匹配到第一个就返回
re.match()     # 必须从字符串开头匹配
re.findall()   # 找出全部匹配内容，返回列表
re.sub()       # 查找并替换
```

## 二、元字符（核心符号）
### 1. 单字符匹配
| 符号 | 含义 |
|------|------|
| `.` | 任意单个字符，**默认不能匹配换行**；`re.DOTALL` 后可以匹配换行 |
| `\d` | 数字 0~9；`\D` 非数字 |
| `\w` | 字母、数字、下划线；`\W` 相反 |
| `\s` | 空白：空格、\n、\t、\r；`\S` 非空白 |
| `[]` | 自定义字符集，`[abc]`匹配a/b/c；`[0-9]`数字；`[^0-9]`取反 |

### 2. 数量限定（控制前面字符出现多少次）
| 符号 | 作用 |
|------|------|
| `*` | 0次、1次、多次 |
| `+` | 至少1次 |
| `?` | 0次或1次；同时开启**非贪婪** |
| `{n}` | 严格n次 |
| `{n,}` | n次及以上 |
| `{n,m}` | n～m次 |

#### 贪婪 vs 非贪婪
- `.*`：贪婪，尽可能抓最长内容
- `.*?`：非贪婪，一满足条件立刻停止（你上面正则里就是这个）

### 3. 位置锚定（不占用字符，只标记位置）
| 符号 | 作用 |
|------|------|
| `^` | 字符串开头 |
| `$` | 字符串结尾 |
| `\b` | 单词边界（英文单词空隙） |

### 4. 分组与捕获
1. `(内容)`：捕获分组，可以单独提取这一段，`group(1)`取值
2. `(?:内容)`：非捕获分组，只用来分组逻辑，不单独提取
3. `\|`：或，`A|B` 匹配A或者B

### 5. 先行断言（你代码里的 `(?=xxx)`）
零宽断言：只判断后面有没有目标文本，**不会把目标文本吞掉**
- `(?=xxx)` 正向先行：后面紧跟着xxx才算匹配（你当前正则用的）
- `(?!xxx)` 负向先行：后面不能是xxx

示例：`aaa(?=bbb)` 匹配aaa，要求aaa后面紧跟着bbb，bbb不会被抓到结果里。

## 三、拆解你这条正则复习一遍
```regex
Thought:\s*(.*?)(?=\nAction:|$)
```
1. `Thought:` 固定文字
2. `\s*` 吃掉后面任意空格
3. `(.*?)` 非贪婪捕获多行内容（依靠re.DOTALL让`.`支持换行）
4. `(?=\nAction:|$)` 碰到换行+Action，或者文本结尾就截止

## 四、常用修饰符 flags（第三个参数）
```python
re.search(pat, text, flags=xxx)
```
1. `re.DOTALL`：`.` 匹配换行符（你正在用）
2. `re.IGNORECASE / re.I`：忽略大小写
3. `re.MULTILINE / re.M`：让 `^` `$` 识别每一行的开头结尾

## 五、原始字符串 r"" 必写规范
正则里大量反斜杠 `\`，Python普通字符串`\\`才代表一个`\`；
`r"正则内容"`原生字符串，不需要转义，写正则一律加 `r""`。
```python
# 推荐
re.search(r"\d+", "123")
# 不推荐
re.search("\\d+", "123")
```

## 六、高频实操模板
### 1. 提取括号内内容
```python
res = re.search(r"\((.*?)\)", text)
```
### 2. 多行截取两段标记中间内容（你的Agent场景标配）
```python
# 截取A和B中间所有内容
re.search(r"A\s*(.*?)(?=B|$)", text, re.DOTALL)
```
### 3. 提取全部数字
```python
nums = re.findall(r"\d+", text)
```
### 4. 替换内容
```python
new_text = re.sub(r"\s+", "", text) # 删除所有空白
```

## 七、易踩坑关键点
1. `.` 默认不匹配换行，多行捕获必须加 `re.DOTALL`；
2. 不加 `?` 的 `.*` 贪婪匹配容易抓过头；
3. `[]` 里面 `. *` 等元字符会失去特殊意义，就是普通符号；
4. `match` 只从头匹配，绝大多数场景优先用 `search`；
5. 断言只是边界判断，不会消耗字符，不会进入捕获结果。

## 八、极简一句话记忆
1. 找单个字符：`\d \w \s . []`
2. 控制次数：`* + ? {}`
3. 锁定头尾：`^ $`
4. 抓一段内容：`()`
5. 限定结尾边界：`(?=xxx)`
6. 多行抓取务必开 `re.DOTALL`